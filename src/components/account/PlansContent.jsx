import PlansTabContent from './PlansTabContent.jsx';

/** @deprecated Use PlansTabContent — mantido para imports legados. */
export default function PlansContent({ user }) {
  return <PlansTabContent user={user} />;
}
