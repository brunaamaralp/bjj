/**
 * Mantido temporariamente: o perfil do aluno vive em `/student/:id` (`StudentProfile`).
 * Reexporta o mesmo componente para qualquer import legado de `StudentPanel`.
 */
export { default as StudentPanel } from '../pages/StudentProfile.jsx';
